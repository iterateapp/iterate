services:
  postgres:
    image: postgres:17
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: iterate
      POSTGRES_PASSWORD: iterate
      POSTGRES_DB: iterate_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
